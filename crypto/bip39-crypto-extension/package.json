{
	"name": "@algorandfoundation/bip39-crypto-extension",
	"version": "0.0.1",
	"description": "Wallet Provider Extension for BIP-39",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"lint": "biome check",
		"lint:fix": "biome check --fix",
		"watch": "tsc -w",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Algorand Foundation",
	"license": "Apache-2.0",
	"dependencies": {
		"@algorandfoundation/wallet-provider": "0.0.1",
		"before-after-hook": "^4.0.0"
	},
	"peerDependencies": {
		"@algorandfoundation/keystore-extension": "0.0.1"
	},
	"devDependencies": {
		"@algorandfoundation/keystore-extension": "0.0.1"
	}
}
